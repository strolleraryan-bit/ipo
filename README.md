# Lot & Ledger — IPO Manager PWA

A single-file, offline-first Progressive Web App for tracking IPO applications, funding, GMP, profit/loss, and money owed between you and the people you apply on behalf of. No build step, no backend, no external database — everything lives in the browser's `localStorage`.

## Files

```
pwa/
├── index.html          ← the entire app (HTML + CSS + JS in one file)
├── sw.js                ← service worker (offline caching)
├── manifest.json        ← PWA manifest (name, icons, theme)
└── icons/
    ├── icon-192.png
    ├── icon-512.png
    ├── icon-maskable-192.png
    └── icon-maskable-512.png
```

**The only file you'll typically need to edit is `pwa/index.html`.** It contains all markup, styles, and logic — the app is intentionally kept as one file for easy deployment (drag-and-drop to Netlify, or open directly in a browser).

## How to run it

- **Locally:** open `pwa/index.html` directly in a browser, or serve the `pwa/` folder with any static file server.
- **Deploy:** drag the `pwa` folder onto Netlify (or any static host). No build command, no environment variables.
- **Install as an app:** once deployed over HTTPS, most browsers offer "Add to Home Screen" / "Install" thanks to `manifest.json` and `sw.js`.

## Data storage

- All data (IPOs, applications, users, transfers, withdrawals) is stored in the browser's `localStorage` under the key `lotledger-app-state`.
- Data is **per-browser, per-device** — it does not sync across devices or accounts on its own.
- Use **Export Backup** (Dashboard → Backup section) regularly. This downloads a `.json` file with the complete app state.
- Use **Import Backup** to restore or move data to another browser/device — just open the app fresh and import the `.json` file.

> If you're editing this project in a different Claude account/session, re-upload this zip there. Any future edits should be made to `pwa/index.html` and the zip re-packaged the same way.

## App structure (inside index.html)

- **State shape:**
  ```js
  state = {
    users: [],          // includes one user with isMe:true (the account owner)
    ipos: [],            // IPO records: name, dates, price, lot size, GMP, listing price, notes
    applications: [],    // one per (user, ipo): lots, profitMode ('commission'|'shared'), fundedByMe (Aryan's
                          // invested amount — always his stake, regardless of mode), amountGiven (total cash
                          // handed over, may exceed fundedByMe), commissionPercent (commission mode only),
                          // bonusToUser (shared mode only — flat extra profit Aryan gives the user above the
                          // ratio split), allotment/sale results. Legacy records use the old `fundingSource`
                          // field ('me'|'shared'|'own') which is auto-migrated on read via profitModeOf().
    transactions: [],    // money transfers between ANY two tracked users (fromUserId/toUserId/amount) —
                          // fully synchronized: deducted from the sender's balance, added to the receiver's,
                          // computed live from this array so deleting a transfer automatically undoes it on
                          // both sides. Every leg touching a non-Aryan user is also tagged fromAccount and/or
                          // toAccount ('A'|'B') — Aryan→user always tags toAccount:'A'; user→Aryan always
                          // tags fromAccount:'A'; an internal move (see below) has fromUserId===toUserId with
                          // differing fromAccount/toAccount. This is also how Aryan actually "collects" money
                          // from a user's Account A.
    withdrawals: [],     // cash withdrawals per user, reduce that user's tracked balance. Non-Aryan entries
                          // carry `account` ('A'|'B'); legacy entries without it default to 'A'.
    deposits: []          // cash deposits for ANY user now (not just Aryan) — "Add Cash to Account". For
                          // Aryan (isMe) this tops up his single liquid balance; for every other user it
                          // always credits their Account B (their own money).
  }
  ```
- **Core money logic** lives in a handful of functions — read these first before making balance-related changes:
  - `computeApp(app, ipo, user)` — the single source of truth for an application's status (pending/holding/refunded/settled), profit split, and `owedToMe` (everything currently owed to Aryan from this application, whether it's principal awaiting allotment result, blocked capital while holding, or settled-but-not-yet-transferred principal+profit) at every stage.
  - `profitModeOf(app)` — resolves an application's profit-sharing mode, migrating legacy `fundingSource` records.
  - `transfersNet(userId)` — generalized, synchronized ledger helper: sum of every transaction where this user is the receiver (+) minus every one where they're the sender (−). Used identically for Aryan and for every friend, and for x↔y transfers between two friends that don't involve Aryan at all.
  - `transfersNetAccount(userId, account)` — same idea, scoped to one sub-account (`'A'`|`'B'`) of a non-Aryan user: only counts a transaction on the side(s) where its `fromAccount`/`toAccount` tag matches. This is what lets an internal A↔B move (same user on both sides) correctly shift money between the two account totals instead of netting to zero.
  - `appAccountALock(app, ipo, user)` / `appAccountBLock(app, ipo, user)` — how much of a single application is currently "locked" out of a non-Aryan user's Account A / Account B respectively: `extraGiven` is always locked out of A (permanently owed until transferred back); the application's Aryan-funded share is locked out of A only while pending/holding and releases automatically (plus profit) once settled; the user's own share (`fullCost − aryanInvested`) is locked out of B the same way, releasing (plus their commission/profit share) once settled. Refunded applications release both locks with no profit/loss.
  - `accountABalance(userId, excludeAppId?)` / `accountBBalance(userId, excludeAppId?)` — a non-Aryan user's two real wallet balances: `accountA = opening + transfersNetAccount(userId,'A') − withdrawalsFor(userId,'A') − Σ appAccountALock(...)`; `accountB = depositsFor(userId) + transfersNetAccount(userId,'B') − withdrawalsFor(userId,'B') − Σ appAccountBLock(...)`. Pass `excludeAppId` to exclude one application's own current lock — used when re-validating balance while editing a still-pending application, so it doesn't count its own existing commitment against itself.
  - `userBalance(userId)` — a friend's full balance record: `{owed, transfers, withdrawn, opening, accountA, accountB, net}` where `net = accountA + accountB` (positive = total money currently tracked in their two accounts).
  - `myDeployedCapital()` — Aryan's capital currently blocked: for friend applications this stays blocked **indefinitely** (pending → holding → refunded → settled) until a real transfer collects it back; for Aryan's own (isMe) applications it's blocked only while pending/holding, releasing automatically at settlement.
  - `myOwnRealizedProfit()` — profit from Aryan's own (isMe) applications only, credited immediately at settlement (no intermediary user account involved).
  - `myAccountBalance()` — Aryan's **liquid** cash only: `opening + deposited − withdrawn + transfersNet(me) − myDeployedCapital() + myOwnRealizedProfit()`. Deliberately does NOT include a friend's unrealized profit/principal — that only lands here once an actual transfer transaction moves it (see "Transfer to Aryan" below).
  - `totalInOthersAccounts()` — dashboard aggregate: sum of `accountABalance(u.id) + accountBBalance(u.id)` across every non-Aryan user.
  - `myTotals()` — dashboard-wide stats (capital deployed, profit, outstanding, IPO counts).
- **The two-account model (Account A / Account B) — every non-Aryan user has exactly two tracked wallets, both real, checkable balances rather than an open-ended receivable:**
  - **Account A** holds Aryan's own money sitting with that user: every transfer Aryan sends them, plus Aryan's principal/profit from their settled or refunded applications that hasn't been transferred back to Aryan yet. The application's opening balance is also treated as Account A. This is the **only** account an application's Aryan-funded share is ever deducted from, and the **only** account a "Transfer to Aryan" is ever drawn from.
  - **Account B** holds the user's own money: cash they've added themselves via "Add Cash to Account" (deposits, now available to every user, not just Aryan), plus their own commission/profit share from settled applications. This is what their own share of a Shared Investment application is deducted from.
  - **Applying to an IPO actually draws down these balances**, rather than creating debt from nothing: Commission Model draws the full applied cost from Account A only; Shared Investment draws Aryan's entered share from Account A and the remaining share (`cost − Aryan's share`) from Account B. `saveApp()` blocks the save (new applicants always; edits only while the application is still pending) if either required account can't cover its share — this is the fix for applicants being added with no money in their account.
  - Money automatically reappears in the relevant account once an application is refunded (full amount, no profit/loss) or settled (profit/commission credited) — no manual "return" step needed, because the per-application lock simply stops being subtracted.
  - **Move Between Account A ↔ B** (`openMoveModal`/`saveMove`) lets any non-Aryan user move their own tracked money between the two accounts — logged as a `state.transactions` entry with `fromUserId===toUserId` and differing `fromAccount`/`toAccount`.
  - **Log Transfer** (`openTxnModal`/`saveTxn`) now includes a From-Account selector whenever the sender is a non-Aryan user: forced to `'A'` (and disabled) when the recipient is Aryan (a "Transfer to Aryan" can only ever come from Account A); otherwise defaults to `'A'` but is user-selectable. The recipient's credited account is derived automatically, never chosen: `'A'` if the sender is Aryan, `'B'` otherwise.
  - **Withdraw to Cash** (`openWithdrawModal`/`saveWithdraw`) includes an Account selector for non-Aryan users so a withdrawal can be taken from either wallet; legacy withdrawals recorded before this model (no `account` field) default to Account A.
- **Profit-sharing modes** for a friend's application (funding source itself is no longer tracked as a separate concept — every application is applied through the user's own two accounts; these two modes decide the split of *both* the deduction and the eventual profit):
  - `'commission'` — the full applied cost is deducted from Account A only (all profit is Aryan's; the user earns a commission % of positive profit as their cut, credited to Account B at settlement). `fundedByMe` is forced to the full applied cost.
  - `'shared'` — both invest (two explicit input boxes: Aryan's Invested Amount, User's Invested Amount, kept in sync to always sum to the applied cost) — Aryan's share is deducted from Account A, the user's share from Account B. Profit splits by investment ratio, credited back to each account at settlement. An optional `bonusToUser` lets Aryan voluntarily shift a flat extra amount of his own share to the user, on top of the ratio split.
  - Applying as Aryan himself (`isMe`) skips profit-sharing and the two-account model entirely — the whole amount and all profit/loss is his, drawn from and realized directly in his own liquid `myAccountBalance()`.
- **Settlement → Transfer to Aryan flow** (structural change): when a friend's application is sold, the full recovered amount (principal + profit, both Aryan's and the user's shares) is treated as landing back in the **user's own two accounts** first — Aryan's share into Account A, the user's share into Account B — exactly like a real IPO refund/payout would. It shows up as part of `owedToMe` / `accountABalance()`, but is **not** added to `myAccountBalance()`. Only when Aryan explicitly transfers it out — via the "Transfer to Aryan" button (transfers the full Account A balance) or "Transfer to Aryan + Extra Amount" (same, plus a manually entered top-up) on the User Detail page — does a real transaction get logged (always debited from Account A), and only then does it appear in Aryan's own account balance. This mirrors the "not allotted → refund lands in user's account" behavior that already existed for rejected applications.
- **Blocked capital while holding**: once an application is allotted but not yet listed/sold, Aryan's invested capital in it is blocked (via `myDeployedCapital()`) and excluded from `myAccountBalance()`. If Aryan funds a *new* application for the same or another user while that capital is still tied up, the new application draws from separate/fresh money — the blocked amount does not silently become available again until it's actually recovered.
- **Money Transfers vs Withdrawals vs Application Funding vs Move Between Accounts** — four distinct concepts, easy to confuse:
  - *Application funding* (`fundedByMe` on an application) is tracked automatically as a live draw on the user's Account A (and Account B in Shared mode) from the moment you fund it — no manual entry needed. `amountGiven` is a separate field for the full cash actually handed over when it's more than Aryan's invested amount (e.g. gave ₹15,000, only ₹14,000 was his stake) — the difference stays permanently owed out of Account A regardless of the IPO's outcome.
  - *Money Transfer* (`state.transactions`) is the only thing that actually moves money between two tracked users' real balances — used both for cash movement unrelated to a specific IPO (advances, repayments) AND as the mechanism that realizes a settled application's profit into Aryan's own account ("Transfer to Aryan" buttons pre-fill and log one of these, always from Account A).
  - *Withdrawal* is money a user takes out of the tracked system entirely as physical cash — it only reduces their own balance (one specific account, for non-Aryan users), doesn't move money to anyone else.
  - *Move Between Accounts* is an internal transfer that moves a single non-Aryan user's own tracked money between their Account A and Account B — doesn't change their total balance, only which account it sits in.

## Recent feature history

- IPO detail: inline GMP quick-edit field, "Download as Image" export (Canvas-based PNG).
- Reports tab: same image export alongside the existing PDF export.
- Users: PAN number (validated format) and TPIN fields, shown masked/plain on the user detail page. Backup/restore includes these.
- Storage: switched from the Claude-Artifacts-only `window.storage` API to `localStorage` (with fallback), fixing data loss on refresh when run as a standalone file.
- Money logic: fixed `computeApp` so amounts funded into an application are tracked as live "owed to me" from pending/holding stages onward, not just after settlement — kept in sync across the applicant card UI, PDF exports, and dashboard totals.
- Layout: fixed bottom padding/margins so the bottom nav no longer overlaps the "+ Log Transfer" and "Backup" buttons on long pages.
- **Withdraw to Cash**: new feature — record cash withdrawals per user (including yourself), which reduce that user's tracked balance without transferring the money to anyone else. Included in backup/restore.
- Layout: fixed the real cause of buttons (Withdraw to Cash, Log Transfer, Backup) hiding behind the bottom nav — the scrollable content lives in `<main>`, not `<body>`, and `main` only had 20px of bottom padding. Increased `main`'s bottom padding to clear the fixed nav bar on every page, including long ones.
- User detail page: merged "Money Transfers" and "Withdrawals to Cash" into a single date-sorted **"Money Activity"** timeline, so every transaction type (transfers in/out, cash withdrawals) is managed chronologically in one place instead of two separate lists. Applied the same withdrawals table to the User Statement PDF export for consistency.
- Renamed the default account-owner seed user from generic "Me" to "Aryan" (existing saves with a user still literally named "Me" are auto-migrated on load).
- Reports tab: added a **"Profit by User"** summary table (settled-app count, total P/L, my share per user) above the existing per-IPO/per-user reports, and the same breakdown appended to the "All IPOs Summary" PDF.
- **Fixed a real money-tracking bug**: `owedToMe` was based only on the amount actually applied to an IPO (`fundedByMe`), so if you handed a user more cash than they ended up applying (e.g. gave ₹15,000, only ₹14,000 went into the IPO), the ₹1,000 difference was never tracked as owed. Added a separate **"Amount Actually Given to User"** field on each application — owed-to-me now correctly reflects the full amount given, not just the amount used, at every application stage (pending/holding/refunded/settled). Older applications without this field fall back to treating `fundedByMe` as the given amount (no behavior change for existing data where the two were equal).
- Fixed a bug in the Add/Edit Applicant modal where, in "Shared Fund" mode, changing the lot count didn't resync the "Amount Given" field to the new investment total (it only updated when the funded-amount input itself was edited directly) — could silently leave a stale given-amount if lots were adjusted after the source was set to shared.
- **Add Cash to Account**: new feature, mirroring Withdraw to Cash but in reverse and only available for Aryan (the account owner, `isMe:true`). Lets you record cash top-ups into your own tracked account — increases `myAccountBalance()`, has no effect on any other user's balance. Shown in the same unified "Money Activity" timeline as transfers and withdrawals, included in the User Statement PDF (Cash Deposits table) and in backup/restore.

- **Renamed app from "Lot & Ledger" to "Aryan's IPO Manager Software"** everywhere: browser `<title>`, H1 header, `apple-mobile-web-app-title` meta, PDF header/footer text, image export header/footer, `manifest.json` name and short_name, `sw.js` cache name (bumped to `v4`). Storage key (`lotledger-app-state`) intentionally kept unchanged to avoid breaking existing user data.
- **Money Activity delete bug fixed**: the ✕ delete button on transfer/withdrawal/deposit items in the Money Activity timeline was also triggering the parent row's `onclick` (which opened the edit modal), making deletion unreliable — sometimes the modal reopened instead of deleting. Fixed with `event.stopPropagation()` on all three delete buttons.
- **Money Activity moved below action buttons**: on the User Detail page, the Log Transfer / Withdraw to Cash / Settle Up / Add Cash buttons now appear above the Money Activity timeline instead of below it — more accessible without scrolling past a long activity log.
- **`deleteUser` cascade expanded**: deleting a user now also removes their `withdrawals` and `deposits` records in addition to applications and transactions, so no orphaned records remain and account totals stay accurate.
- **Backup restore made forward-compatible**: restore now preserves any extra top-level keys present in the backup that aren't in the current known schema (useful when restoring a backup from a newer version into an older app). Success toast now shows restored user and IPO counts.
- **Per-user detailed PDF report** (`exportUserCardPDF`): new "⬇ Download User Report" button on every User Detail page. Exports a single-user PDF covering: Name, PAN, TPIN; Balance (opening, net, owed to Aryan); IPO Activity summary (total applied, allotted, not allotted/pending, commission/profit earned by Aryan); full per-IPO application breakdown (IPO name, lots, status, owed to Aryan, profit/commission). Available for all users including the account owner.
- **All Users PAN & TPIN directory PDF** (`exportAllUsersPDF`): new "⬇ Download All Users (PAN & TPIN)" button at the top of the Users List page (hidden if no non-me users). Exports a single PDF table with Name, PAN, and TPIN for every user (excluding Aryan). Useful for quick reference when filing applications.
- **Money Activity delete fixed (root cause)**: `confirm()` dialogs in `deleteTxn`, `deleteWithdraw`, and `deleteDeposit` are suppressed silently in PWA standalone mode on Android/iOS, causing taps on the delete button to appear to do nothing. Removed `confirm()` from all three inline delete functions. The "Delete" button inside the edit modal (opened by tapping the row) still exists as the confirmation step for users who want it. Also replaced the tiny `✕` span with a proper `<button>` element with a larger tap target (padding 6px 8px) so the delete is reliably hittable on mobile.
- **IPO Details report/image no longer shows per-applicant expected profit**: `exportIpoDetailsPDF` and `exportIpoDetailsImage` now show only IPO-level info (overview table + GMP-based expected profit per share / per lot) — the "Expected Profit by Applicant" breakdown table has been removed from both. Per-applicant profit is still visible on the individual applicant cards in the IPO detail view.
- **Fixed remaining native `confirm()` dialogs**: `deleteApp` (remove applicant), `deleteIpo`, `deleteUser`, and the backup-import handler still used `confirm()`, which is silently suppressed in standalone PWA mode on Android/iOS (same root cause documented in the earlier Money Activity delete fix) — meaning taps on "Remove"/"Delete IPO"/"Delete User"/importing a backup could silently do nothing. Added a new `confirmAction(message, onConfirm)` helper — a proper in-app modal with Cancel/Delete buttons — and routed all four through it instead of `window.confirm()`. `sw.js` cache bumped to `v5` so installed PWAs pick up the fix.
- **Major money-model restructuring — synchronized transfers, blocked capital, and settle-before-realize profit:**
  - **Generalized transfer sync**: `state.transactions` used to only affect balances when Aryan was one of the two parties (`transfersWithMe`). Replaced with `transfersNet(userId)`, a symmetric helper used for every user including Aryan — any x→y transfer now correctly deducts from x's balance and adds to y's, for any pair of tracked users, not just transfers touching Aryan. Because it's computed live from `state.transactions`, deleting a transfer automatically reverses its effect on both sides.
  - **Removed the 3-way "Funding Source" toggle** (100% My Fund / Shared Fund / User's Own Fund) from the Add/Edit Applicant modal. Funding is now always understood to run through the user's own account; instead there are two **Profit Sharing** modes selected via radio-style toggle: **Commission Model** (all profit is Aryan's, user earns a commission % — same math as the old "me" mode) and **Shared Investment** (two explicit, bidirectionally-linked input boxes — Aryan's Invested Amount / User's Invested Amount, always summing to the applied cost — with profit split by that ratio, plus an optional **Bonus to User** flat top-up Aryan can grant above the ratio split). The old "User Investment Amount" read-only field is gone, replaced by the editable User's Invested Amount box. New field `app.profitMode` (`'commission'|'shared'`) and `app.bonusToUser` added; old `app.fundingSource` values are auto-migrated for display via `profitModeOf()` (`'own'` records become `'shared'` with `fundedByMe:0`, which naturally computes a zero profit share — no behavior change for existing data).
  - **Profit no longer auto-credits to Aryan's account at settlement.** Previously `myAccountBalance()` added `myTotals().profitMine` the instant an application was marked sold, even though nothing had physically moved. Now, a settled friend application's principal + profit is treated as landing in the **user's own tracked account** (same place a real IPO refund lands) — visible in their "owed to you" balance — and Aryan's own account balance is untouched until he actually collects it.
  - **New "Transfer to Aryan" flow on the User Detail page**: the existing Settle Up button is now labeled **"Transfer to Aryan"** and shows the live amount owed; a new **"Transfer to Aryan + Extra Amount"** button opens a small modal for an optional top-up amount, then logs one transfer transaction for owed + extra. Both routes are the *only* way a friend's profit/principal becomes part of `myAccountBalance()` — implemented via `openTransferExtraModal()` / `confirmTransferExtra()`, reusing the existing transfer modal underneath.
  - **New `myDeployedCapital()` / `myOwnRealizedProfit()` helpers** feed into a rewritten `myAccountBalance()` = `opening + deposited − withdrawn + transfersNet(me) − myDeployedCapital() + myOwnRealizedProfit()`. Capital Aryan puts into a friend's application stays blocked (excluded from his liquid balance) indefinitely — through pending, holding, refunded, and settled stages — until a real transfer collects it back; so funding a *new* application while an old one is still unsettled correctly draws down fresh balance rather than double-counting still-tied-up money. Aryan's own (isMe) applications are only blocked while pending/holding and release automatically at settlement, since there's no intermediary account for his own applications.
  - `totalInOthersAccounts()` rewritten to match: opening balance + capital received (Aryan's invested/given amounts across their applications) + their own retained profit share + `transfersNet(userId)` − withdrawals.
  - Dashboard "Money" section help text updated to explain the new liquid-vs-held distinction. IPO applicant cards, PDF/CSV exports, and the Reports tab updated to read `c.profitMode` instead of `c.fundingSource` (labels: "Commission Model" / "Shared Investment").
  - `sw.js` cache bumped to `v6` so installed PWAs pick up the new applicant modal and money logic on next load.

- **Balance check before adding applicant**: `saveApp()` now checks whether the selected user (or Aryan himself) has a positive account balance before allowing a new applicant entry. If the balance is ≤ 0, a warning toast is shown ("add funds first via Log Transfer / Add Cash to Account") and the save is blocked. Edits to existing applicants are not blocked. A live balance indicator (green if positive, red if ≤ 0) is also shown inside the Add Applicant modal below the user selector, updating whenever the user selection changes.
- **Fixed `currentMode` scope bug in Add/Edit Applicant modal**: `saveApp()` is a top-level function and could not access the `let currentMode` closure variable inside `openAppModal()` — `typeof currentMode` evaluated to `'undefined'` every time, silently falling back to `'commission'` regardless of which mode the user picked. Fixed by writing `window._appModalMode` alongside `currentMode` whenever it's set (on init and in `setProfitModeUI`), and having `saveApp()` read `window._appModalMode` instead. Shared Investment mode now saves correctly.
- **`showToast()` now accepts an optional `duration` (ms) second argument** — defaults to 1800ms; the new balance-warning toasts use 5000ms so the message is readable.

- **Partial allotment support**: Edit Applicant modal now shows a "Lots Actually Allotted" field when an application has been marked allotted — lets you set a smaller number than applied (e.g. applied 2 lots, got 1). `saveApp()` reads and saves this back to `lotsAllotted`, which `computeApp()` already uses for profit/proceeds calculations.
- **User detail balance grid now shows Transfers Net**: The stat-grid on a non-me user's detail page now has a 5th tile — "Transfers Net" (signed, green if positive) — so advances and repayments are visible at a glance without scrolling to Money Activity.
- **`exportUserCardPDF` ("⬇ Download User Report") now includes Money Transfers, Withdrawals, and Cash Deposits** — same sections already present in `exportUserReportPDF` (Reports tab). The two user-level PDFs are now consistent and complete.
- **Fixed misleading column header in `exportUserCardPDF`**: "Commission / Share Earned (Aryan)" was renamed to "Aryan's Profit Share" — it was showing `myProfitShare` (Aryan's cut), not the user's commission, so the label was wrong.

- **Major restructuring — two real, checkable accounts (A/B) per non-Aryan user, replacing the old single open-ended "owed" balance (v5.4.5):**
  - **Account A** (Aryan's money with the user: transfers he's sent them + his uncollected profit/principal) and **Account B** (the user's own money: cash they've added via the now-generalized "Add Cash to Account" + their own profit/commission share) are now real wallets with checkable balances (`accountABalance()`/`accountBBalance()`), not an unbounded receivable. `userBalance(userId).net` is now `accountA + accountB`.
  - **Applying to an IPO actually deducts from these accounts** instead of creating debt from nothing: Commission Model draws the full cost from Account A only; Shared Investment splits the draw between Account A (Aryan's entered share) and Account B (the remainder). Money reappears automatically (plus profit, minus nothing on refund) once the application resolves.
  - **Fixes the "applicant added with no money in their account" bug**: `saveApp()` now hard-blocks adding a new applicant — or editing one that's still pending — if the relevant account(s) (chosen by profit-sharing mode) can't actually cover the required amount, with a specific toast naming which account and how much is missing. The old check only looked at whether the user's combined balance was `<= 0`, regardless of the actual amount being applied; the new check compares the exact required amount per account.
  - **Add Cash to Account** is no longer Aryan-only — every user can now log a cash deposit, which always credits their Account B.
  - **Log Transfer** gained a From-Account selector for non-Aryan senders; a transfer to Aryan is always forced to debit Account A; a transfer from Aryan always credits the recipient's Account A; a transfer between two non-Aryan users credits the recipient's Account B.
  - **New "Move Between Account A ↔ B"** feature (`openMoveModal`/`saveMove`) lets a user shift their own tracked money between the two accounts without changing their total.
  - **Withdraw to Cash** gained an Account selector for non-Aryan users.
  - **"Transfer to Aryan" / "Transfer to Aryan + Extra Amount"** now pull from Account A only (not the old combined net balance) — matches the model where Account B is exclusively the user's own money and is never swept to Aryan automatically.
  - User Detail page, `exportUserReportPDF`, and `exportUserCardPDF` all updated to show Account A / Account B balances (instead of a single net figure), and to tag Money Transfers/Withdrawals with which account moved. Dashboard "Money" section help text updated to explain the two-account model.
  - `totalInOthersAccounts()` rewritten as the sum of `accountABalance() + accountBBalance()` across all non-Aryan users.
  - `sw.js` cache bumped to `v13` so installed PWAs pick up the new applicant modal and money logic on next load.

- **New IPO logo everywhere**: replaced the old "L" avatar mark with a bold gold "IPO" wordmark on a dark bordered box. Applied consistently across: app header (SVG inline), browser tab (inline SVG favicon), all PDF reports (logo mark drawn in the header bar alongside the app name), canvas image exports (header bar), and all PWA icons (192/512 any + maskable — regenerated as PNG with the same dark box / gold IPO / gold underline design). `sw.js` cache bumped to `v8`.

- **Four bug fixes (v5.4.1):**
  - `exportIpoDetailsImage`'s logo mark used native `ctx.roundRect()` for the stroke outline, which crashes on older Android WebView (no native support). Added a path-only `roundRectPath(ctx,...)` helper (manual quadratic-curve path, no `fill()`) that both the existing `roundRect()` (now a thin wrapper: path + fill) and the stroke-only outline use — neither relies on the native canvas API anymore.
  - `exportUserCardPDF`'s header underline used `doc.setGState(new doc.GState({opacity:0.4}))` to fade the gold underline — `jsPDF.GState` isn't reliably available across jsPDF builds/versions and could throw. Removed the GState/opacity calls entirely; the underline is now drawn in a fixed dimmer gold color (`rgb(120,96,55)`) to get the same muted look without any opacity API dependency.
  - "Sell available on listing day" was shown for any allotted-and-holding application whenever `isListingDay` was falsy — which is also true when the IPO has **no** listing date set at all, so the message appeared (and the "Enter Sell Price" button never appeared) even before a listing date existed. Split the condition: the message now only shows when `ipo.listingDate` is set but hasn't arrived yet; a new "Set listing date to enable selling" message covers the no-date-set case instead.
  - `window._appModalMode` (the window-level mirror of the Add/Edit Applicant modal's profit-sharing mode, read by `saveApp()`) was only ever written by `openAppModal`/`setProfitModeUI`, and `openModal()` didn't guard against being called while a modal was already open — quickly opening a second modal stacked a duplicate `#modalBackdrop` in the DOM instead of replacing it, leaving the first modal's stale state (including `window._appModalMode`) still reachable underneath. `openModal()` now calls `closeModal()` first (so modals never stack) and resets `window._appModalMode = null` before building the new modal; `openAppModal` immediately sets its own value right after, so this only affects non-app modals or fast double-opens.
  - `sw.js` cache bumped to `v9` so installed PWAs pick up these fixes.

- **Six data-safety and validation improvements (v5.4.2):**
  - **Instant save on close/backgrounding**: `saveState()` debounces writes by 120ms, so an edit made right before closing/backgrounding the app could be lost. Added `flushSaveNow()` — writes to `localStorage` synchronously (and best-effort to `window.storage` if present) bypassing the debounce — wired to `visibilitychange` (hidden), `pagehide`, and `beforeunload`.
  - **Backup reminder**: new `lotledger-last-backup-at` localStorage key (kept outside `state`, not part of export/import) records when `exportData()` last ran. Dashboard's Backup card now shows "Last backup: N days ago" (or "No backup taken yet"), turning red/urgent past 14 days or if never backed up.
  - **Automatic rotating restore points**: new `lotledger-autobackups` localStorage key holds up to the 3 most recent full-state snapshots (`pushAutoBackup(reason)`, trimmed via `while(list.length>3) list.shift()`), independent of manual export. A snapshot is taken automatically right before an import is applied (`'before-import'`) and at most once per day on app load (`maybeDailyAutoBackup()`, called from `loadState()`). Dashboard's Backup card lists these with a "Restore" button (`restoreAutoBackup(index)`, confirmed via `confirmAction`).
  - **Import validation**: new `validateBackupShape(parsed)` checks the file is a plain object with valid `users`/`ipos` arrays (each entry needs `id`+`name`), that `applications`/`transactions`/`withdrawals`/`deposits` are arrays when present, that every application has an `id`, and that any `userId`/`ipoId` an application references actually exists in the same file. Import is rejected with a specific toast message before the confirm step if any check fails, instead of the old bare "not a valid backup file" check (which only verified `users`/`ipos` were arrays).
  - **Input validation on Add/Edit IPO**: `saveIpo()` now rejects price ≤ 0, lot size ≤ 0, a non-numeric GMP, and a listing date set before the allotment date, each with a specific toast — previously these silently saved as 0/negative and could produce nonsensical downstream costs.
  - **Input validation on Add/Edit Applicant and Sell Price**: `saveApp()` now rejects a commission % outside 0–100, and caps "Lots Actually Allotted" to between 0 and the lots applied for (previously could be set higher than lots applied, or negative). `saveSellPrice()` and `saveListingPrice()` now reject negative prices.
  - `sw.js` cache bumped to `v10` so installed PWAs pick up these fixes.

- **Two follow-up bug fixes on the above (v5.4.3):**
  - `restoreAutoBackup()` overwrote current state with a snapshot with no way to undo the restore itself — if you tapped "Restore" on the wrong one, whatever was current before that tap was gone for good. Now takes its own automatic snapshot (`reason:'before-restore'`) of the current state right before restoring, so a wrong restore can also be undone.
  - `confirmAction()`'s confirm button was hardcoded to read "Delete", which is correct for the four actual deletion call sites (`deleteApp`, `deleteIpo`, `deleteUser`, and the withdraw-cash flow) but confusing/alarming when reused for Import Backup and Restore Point (neither of which reads as a "delete"). Added an optional third `okLabel` parameter (defaults to `'Delete'` — every existing call site is unaffected); the import and restore confirmations now pass `'Import'` / `'Restore'`.
  - `sw.js` cache bumped to `v11` so installed PWAs pick up these fixes.

- **Bug fix: long IPO notes got clipped off the bottom of "Download as Image" (v5.4.4).** `exportIpoDetailsImage()` sized its canvas using a fixed guess (`48 + 26px`) for the whole Notes block, regardless of how many lines the note actually word-wrapped into — good for roughly one line, but a longer note wrapping into 2-3+ lines pushed the notes text and the footer below the canvas's actual pixel height, silently cropping them out of the downloaded PNG (canvas drawing calls past the canvas's own bounds are just dropped, not scaled/resized). Fixed by measuring the notes wrap (`wrapText`) against a small scratch canvas *before* the real canvas is sized, so `estimatedH` accounts for the true line count; the real drawing pass reuses that same `noteLines` array instead of re-wrapping, so what's measured is exactly what's drawn.
  - `sw.js` cache bumped to `v12` so installed PWAs pick up this fix.

## Known conventions to keep in mind

- Money values are stored as plain numbers (rupees, not paise).
- Dates are ISO strings (`YYYY-MM-DD`).
- IDs are generated with `uid()`, a simple random-string helper — not UUIDs.
- The service worker auto-versions its cache; bump the cache name in `sw.js` if you need to force clients to pick up a new `index.html` immediately (otherwise it updates on next reload after the new service worker activates).
- **A friend's application "profit"/principal is not the same as Aryan's account balance.** Settled amounts sit in the user's two tracked accounts (Aryan's share in `accountABalance()`, the user's share in `accountBBalance()`) until an actual transfer (`state.transactions` entry) moves them into `myAccountBalance()`. Never make a balance-related change that credits Aryan's account directly from `computeApp()` output without going through a real transfer — that reintroduces the "profit shows before it's actually collected" bug this was fixed to avoid.
- **Backup/restore compatibility**: `profitMode` and `bonusToUser` are additive fields on `applications`; `fromAccount`/`toAccount` on `transactions`, `account` on `withdrawals`, and non-Aryan entries in `deposits` are additive too — old backups without them still load fine (`profitModeOf()` falls back to the legacy `fundingSource` field; `bonusToUser` defaults to no bonus; missing `fromAccount`/`toAccount` are only relevant to display and don't break balance math since `transfersNetAccount` simply won't match untagged legacy transactions against either account; missing `account` on a withdrawal defaults to `'A'`). No existing field was removed or renamed, so old backups remain fully restorable — though balances computed from a very old backup will reflect the new two-account rules going forward, not a re-derivation of history.
- **Two new localStorage keys live outside `state` entirely** (not part of `lotledger-app-state`, not included in Export/Import Backup): `lotledger-last-backup-at` (ISO timestamp of the last manual export, drives the dashboard backup reminder) and `lotledger-autobackups` (array of up to 3 `{ts, reason, data}` automatic full-state snapshots, taken before imports and at most once/day, used only for in-app "Restore" — separate concept from a manual Export Backup `.json` file).

---

## ⚠️ INSTRUCTIONS FOR CLAUDE — READ BEFORE MAKING ANY CHANGE

This project is worked on across multiple Claude accounts/sessions over time. To keep it self-contained and never fall out of sync, follow this process **every single time**, no matter how small the change:

1. **Before editing:** read this entire README first. It's the single source of truth for the app's structure, data model, and history — don't skip it even for a "quick fix."
2. **Make the code change(s)** in `pwa/index.html` (the only file with app logic; `sw.js`/`manifest.json`/icons rarely need touching unless the request is specifically about PWA installability, offline caching, or icons).
3. **Update this README.md in the same turn**, not as an afterthought:
   - Add a bullet to "Recent feature history" describing what changed and why (short, factual, no marketing language).
   - If the change adds/renames/removes a field in `state` (users, ipos, applications, transactions, withdrawals, or a new top-level key), update the **State shape** code block above.
   - If the change adds a new core money-calculation function or changes what an existing one returns, update **Core money logic**.
   - If the change introduces a new concept a future editor could confuse with an existing one (like Money Transfer vs Withdrawal vs Application Funding), add it to **Known conventions**.
4. **Always rebuild the full zip**, not just hand over `index.html` alone:
   - Copy the latest `index.html` into `pwa/index.html` inside the working output folder.
   - Re-zip the entire `pwa/` folder **and** this `README.md` together into one `.zip`.
5. **Always deliver the zip and the standalone `index.html` together, in the same reply, every single time** — this is a hard rule, not a suggestion. Never send just the zip. Never send just the HTML file. Never send them in separate turns ("here's the zip, I'll send the HTML next"). Both files must:
   - Be built from the exact same edited version of `index.html` — copy it once, then use that same copy both inside the zip (`pwa/index.html`) and as the loose standalone file.
   - Be presented in the same `present_files` call (or same reply) so the person always receives a matching pair.
   - This applies even if the person's request only explicitly mentions one of the two (e.g. "update the html" or "give me the zip") — still deliver both, unless the person has explicitly said they only want one going forward.
6. **Never let the README drift from the code.** If you're unsure whether a change is "big enough" to document, document it — a stale README is worse than a slightly over-detailed one. The person may open this project in a completely different Claude session with zero conversation history; that session's only context will be this file plus the code itself.
7. **Even if the person asks for "just the HTML file" or "just the zip,"** still deliver both together per rule 5, unless they've explicitly said they only want one going forward for the rest of the session. Defaulting to both is safer than guessing wrong.

